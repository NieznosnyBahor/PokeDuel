0. Spis treści
1. Przebieg gry

Utworzenie planszy
Utworzenie 2 graczy
Losowanie pokemonów
Losowanie kto pierwszy
Kolejka az do konca

2.


InputStream stream = PokeDuel.class.getResourceAsStream("Database.txt");
        Scanner sc = new Scanner(stream);
        while (sc.hasNext())
                {
                    System.out.println(sc.nextLine());
                }

 

        Integer[] array1 = {1,2,3};
        Integer[] array2 = {1,2,3};
        
        Map<String, Integer[]> Oceny = new HashMap<String, Integer[]>();
        Oceny.put("Matematyka", array1);
        Oceny.put("Polski", array2);

Zrobic metode porownawczą dla pokemonów w klasie Game
